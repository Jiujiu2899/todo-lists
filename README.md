1.Tech stack use express.js for backend and mongoDB for Database

2.Fontend use React.js 

3.Depoly Backend server by Render

4.Depoly Fontend client by Vercel

5.run backend server npm start

6.run react.js npm run dev

//This is the error I'm encountering, 
   1) I first used MongoDB Compass for the backend, but data submission via Postman failed, so I switched to MongoDB Atlas.
   2) The client is unable to fetch data from the backend due to an unresolved issue; the response keeps returning null.
   3) Fixing the backend took a lot of time because of issues I’d never encountered before. I tried to fix it myself to truly understand it, but with limited time, I turned to ChatGPT for help—and then the entire code just broke immediately.
   4) The UI is incomplete because the backend connection is down. I can't fetch real user data, so I'm using a mock object to show data on the dashboard for now
   5) In the end, I tried the final step, which was deploying, but it broke once again during the backend deployment.
   6) I can't access the dashboard after the backend deployment. The login fails because the API URL changed, blocking my access to the dashboard.


